using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SkillPosColorChange : MonoBehaviour
{
    private void OnTriggerEnter(Collider other)
    {
        if(other.tag == "clone")
        {

        }
    }
}
